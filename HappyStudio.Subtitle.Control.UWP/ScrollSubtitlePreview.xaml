<local:SubtitlePreviewControlBase
    x:Class="HappyStudio.Subtitle.Control.UWP.ScrollSubtitlePreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HappyStudio.Subtitle.Control.UWP"
    xmlns:model="using:HappyStudio.Parsing.Subtitle.Interfaces"
    xmlns:temp="using:HappyStudio.Subtitle.Control.UWP.ItemTemplates"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <ScrollViewer x:Name="Root_ScrollViewer">
        <ListView x:Name="Main_ListView" ItemsSource="{x:Bind Source, Mode=OneWay}" SelectionMode="Extended" IsItemClickEnabled="True" ItemClick="Main_ListView_OnItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:ISubtitleLine">
                    <temp:ScrollPreviewItemTemplate Text="{x:Bind Content, Mode=OneWay}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </ScrollViewer>
</local:SubtitlePreviewControlBase>
